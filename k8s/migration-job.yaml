apiVersion: batch/v1
kind: Job
metadata:
  name: sso-migration
  namespace: portfolio
  labels:
    app.kubernetes.io/name: sso-migration
    app.kubernetes.io/part-of: portfolio
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sso-migration
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: ghcr.io/szymonborowski/microservices-sso:v0.0.1
          imagePullPolicy: Never
          command: ["php", "artisan", "migrate", "--force"]
          envFrom:
            - configMapRef:
                name: sso-config
            - secretRef:
                name: sso-secret
