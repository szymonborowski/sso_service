apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sso
  namespace: portfolio
  labels:
    app.kubernetes.io/name: sso
    app.kubernetes.io/part-of: portfolio
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sso.borowski.services
      secretName: sso-tls
  rules:
    - host: sso.borowski.services
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sso
                port:
                  number: 80
