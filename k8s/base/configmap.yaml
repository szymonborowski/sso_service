apiVersion: v1
kind: ConfigMap
metadata:
  name: sso-config
  namespace: portfolio
  labels:
    app.kubernetes.io/name: sso
    app.kubernetes.io/part-of: portfolio
data:
  APP_NAME: "SSO"
  APP_ENV: "production"
  APP_DEBUG: "false"
  APP_URL: "https://sso.borowski.services"
  LOG_CHANNEL: "stderr"
  DB_CONNECTION: "mysql"
  DB_HOST: "sso-mysql"
  DB_PORT: "3306"
  # Users service configuration
  USERS_API_URL_INTERNAL: "http://users"
  USERS_SERVICE_API_KEY: "internal-api-key-change-in-production"
  # Allowed post-logout redirect hosts (comma-separated)
  ALLOWED_REDIRECT_HOSTS: "https://borowski.services,https://admin.borowski.services"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sso-nginx-config
  namespace: portfolio
  labels:
    app.kubernetes.io/name: sso
    app.kubernetes.io/component: nginx
    app.kubernetes.io/part-of: portfolio
data:
  default.conf: |
    server {
        listen 80;
        server_name _;

        root /var/www/html/public;
        index index.php;

        # Health probes dla Kubernetes
        location = /health { return 200 "ok\n"; add_header Content-Type text/plain; }
        location = /ready  { return 200 "ok\n"; add_header Content-Type text/plain; }

        # Vite build assets (fingerprinted filenames -- long cache)
        location /build/ {
            expires    1y;
            add_header Cache-Control "public, immutable";
            access_log off;
            try_files  $uri =404;
        }

        # Static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
            expires    30d;
            add_header Cache-Control "public, no-transform";
            access_log off;
            try_files  $uri =404;
        }

        # Laravel front controller
        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        # PHP-FPM (sidecar -- same pod network namespace)
        location ~ \.php$ {
            fastcgi_pass            127.0.0.1:9000;
            fastcgi_index           index.php;
            include                 fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
            fastcgi_param DOCUMENT_ROOT   $realpath_root;
            fastcgi_buffers         16 16k;
            fastcgi_buffer_size     32k;
            fastcgi_read_timeout    60s;
        }

        location ~ /\.(?!well-known).* {
            deny all;
        }
    }
