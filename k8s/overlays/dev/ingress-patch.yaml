apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sso
  namespace: portfolio
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls: []
  rules:
    - host: sso.portfolio.kube
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sso
                port:
                  number: 80
